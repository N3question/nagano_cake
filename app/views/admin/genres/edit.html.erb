<div class="container my-5 ">
  <div class="row">
    <div class="">
      <h5 class="ml-5 mb-5">ジャンル編集</h5>
        <% form_with model: [:admin, @genre], local: true do |f| %>
          <label>ジャンル名</label>
          <% f.text_field :name %>
          <% f.submit '変更を保存', class:"btn btn-success" %>
        <% end %>
    </div>
  </div>
</div>

